# Optional local database connection for future extensions.
# Keep real credentials in an untracked .env file, never in source control.
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/movie_league

